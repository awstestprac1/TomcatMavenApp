pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo "testing pipeline"
                sh 'pwd'
                sh 'ls -ltr'
            }
        }
    }
}
